extends layout

block content
  h1= title
  p Enter a github username to search

  form(method='POST' action='/search')
    div.form-group
      label(for='username')
      input#screen_name.form-control(type='text', placeholder='Github Username' name='username')
      input#search_button.form-control(type='submit', value='Search')
  
